@inherits HomePageComponent
@page "/"

@if (State == GameState.Menu)
{
    <Menu OnCreateGame="@OnCreateGame" />
}
else if (State == GameState.Game)
{
    <Board Game="@Game" OnPlayAgainClicked="@OnShowMenu" Debug="@Debug" />
}
